import functions

def main():

    # test values
    x = 5
    arr = [11, 102, 88, 99]

    assert functions.square(x) == 25, "SQUARE failed."
    #assert functions.mean(arr) == 75, "MEAN failed."
    #assert functions.max(arr) == 101, "MAX failed."
    #assert functions.min(arr) == 11, "MIN failed."
    #assert functions.sum(arr) == 300, "SUM failed."

main()
